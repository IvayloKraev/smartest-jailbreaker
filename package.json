{
  "name": "smartest-jailbreaker",
  "version": "1.0.0",
  "description": "Jailbreaks SmarTest and exits strict mode",
  "main": "dist/index.js",
  "scripts": {
    "build:typecript": "tsc",
    "build:public": "cp -r ./public/* ./dist",
    "build": "bun run build:typecript && bun run build:public"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/chrome": "^0.0.315"
  },
  "private": true
}
